# Model

This directory is reserved for SegFormer model assets, checkpoints, and model-facing documentation.

The backend currently does not load or run inference. Future implementation should add a dedicated model service layer before exposing prediction endpoints.

